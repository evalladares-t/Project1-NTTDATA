server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/evalladares-t/Project1-NTTDATA.git
          search-paths: config-data
          username: evalladarestav
          password: evalladarestav
          default-label: "develop"
  security:
    user:
      name: root
      password: s3cr3t
